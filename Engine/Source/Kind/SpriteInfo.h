#pragma once

namespace Lion
{
    struct SpriteInfo
    {
        float x, y;
        float scale;
        float depth;
        float rotation;
        uint  width;
        uint  height;
        GLuint texture;
    };
}
